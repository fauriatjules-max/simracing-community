/* Reset et base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Montserrat', Arial, sans-serif;
    background: linear-gradient(135deg, #FF0000 0%, #000000 100%);
    color: #FFFFFF;
    min-height: 100vh;
}

.app-container {
    max-width: 414px;
    margin: 0 auto;
    min-height: 100vh;
    background: inherit;
    position: relative;
}

/* En-têtes */
.main-header, .game-header, .chat-header, .auth-header, .profile-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 15px;
    background: rgba(0, 0, 0, 0.8);
    position: sticky;
    top: 0;
    z-index: 100;
}

.logo {
    font-weight: 700;
    font-size: 20px;
}

.app-title {
    font-size: 16px;
    font-weight: 600;
}

.profile-icon, .new-chat-btn {
    background: none;
    border: none;
    color: white;
    font-size: 20px;
    cursor: pointer;
}

.back-button {
    background: none;
    border: none;
    color: white;
    font-size: 20px;
    cursor: pointer;
}

/* Contenu principal */
.welcome-container, .games-container, .chat-container, .auth-container, .profile-container {
    padding: 20px;
    min-height: calc(100vh - 120px);
}

.welcome-content {
    text-align: center;
    margin-top: 50px;
}

.welcome-content h2 {
    font-size: 24px;
    margin-bottom: 10px;
}

.welcome-content p {
    margin-bottom: 30px;
    opacity: 0.8;
}

/* Listes de jeux */
.games-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.game-card {
    display: flex;
    align-items: center;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    padding: 15px;
    height: 120px;
    border-left: 4px solid #CC0000;
}

.game-logo {
    width: 80px;
    height: 80px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    margin-right: 15px;
}

.game-name {
    font-size: 18px;
    font-weight: 700;
    flex: 1;
}

/* Navigation inférieure */
.bottom-nav {
    display: flex;
    justify-content: space-around;
    background: rgba(0, 0, 0, 0.9);
    padding: 10px 0;
    position: sticky;
    bottom: 0;
}

.nav-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 5px 15px;
    border-radius: 20px;
    cursor: pointer;
    transition: background 0.3s;
}

.nav-item.active {
    background: rgba(255, 0, 0, 0.3);
}

.nav-item span:first-child {
    font-size: 20px;
    margin-bottom: 2px;
}

.nav-item span:last-child {
    font-size: 12px;
}

/* Détails du jeu */
.game-banner {
    text-align: center;
    padding: 20px;
}

.game-logo-large {
    width: 150px;
    height: 150px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 15px;
    margin: 0 auto 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
}

.game-title-large {
    font-size: 24px;
    font-weight: 700;
}

/* Onglets */
.tabs-container {
    background: rgba(0, 0, 0, 0.8);
}

.tabs {
    display: flex;
}

.tab-button {
    flex: 1;
    background: none;
    border: none;
    color: white;
    padding: 15px;
    font-size: 14px;
    cursor: pointer;
    border-bottom: 2px solid transparent;
}

.tab-button.active {
    border-bottom-color: #FF0000;
}

.tab-pane {
    display: none;
    padding: 15px;
}

.tab-pane.active {
    display: block;
}

/* Circuits */
.circuits-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.circuit-card {
    display: flex;
    align-items: center;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 12px;
}

.circuit-image {
    width: 100px;
    height: 60px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 6px;
    margin-right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 10px;
}

.circuit-info {
    flex: 1;
}

.circuit-name {
    font-weight: 600;
    margin-bottom: 5px;
}

.circuit-details {
    font-size: 12px;
    opacity: 0.8;
}

/* Posts */
.posts-container {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.post-card {
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    padding: 15px;
}

.post-header {
    display: flex;
    align-items: center;
    margin-bottom: 10px;
}

.user-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    margin-right: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.user-info {
    flex: 1;
}

.username {
    font-weight: 600;
    margin-bottom: 2px;
}

.post-date {
    font-size: 12px;
    opacity: 0.7;
}

.post-content {
    margin-bottom: 10px;
    line-height: 1.4;
}

.post-video {
    width: 100%;
    height: 200px;
    background: rgba(0, 0, 0, 0.5);
    border-radius: 8px;
    margin: 10px 0;
    display: flex;
    align-items: center;
    justify-content: center;
}

.post-actions {
    display: flex;
    gap: 15px;
}

.action-btn {
    background: none;
    border: none;
    color: white;
    display: flex;
    align-items: center;
    gap: 5px;
    cursor: pointer;
    font-size: 14px;
}

/* Bouton flottant */
.floating-add-btn {
    position: fixed;
    bottom: 80px;
    right: 20px;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: #FF0000;
    border: none;
    color: white;
    font-size: 24px;
    cursor: pointer;
    box-shadow: 0 4px 15px rgba(255, 0, 0, 0.3);
}

/* Modal */
.modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    z-index: 1000;
}

.modal-content {
    background: linear-gradient(135deg, #FF0000 0%, #000000 100%);
    margin: 15% auto;
    padding: 20px;
    border-radius: 15px;
    width: 90%;
    max-width: 400px;
}

.close {
    float: right;
    font-size: 24px;
    cursor: pointer;
}

.modal-content textarea {
    width: 100%;
    height: 120px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 8px;
    color: white;
    padding: 12px;
    margin: 15px 0;
    resize: none;
    font-family: inherit;
}

.modal-content textarea::placeholder {
    color: rgba(255, 255, 255, 0.6);
}

.post-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.icon-btn {
    background: none;
    border: none;
    color: white;
    font-size: 20px;
    cursor: pointer;
}

.post-btn {
    background: #FF0000;
    border: none;
    color: white;
    padding: 8px 20px;
    border-radius: 20px;
    cursor: pointer;
}

/* Authentification */
.auth-form {
    background: rgba(255, 255, 255, 0.1);
    padding: 20px;
    border-radius: 15px;
    margin-top: 30px;
}

.input-group {
    margin-bottom: 20px;
}

.input-group label {
    display: block;
    margin-bottom: 8px;
    font-size: 14px;
    font-weight: 500;
}

.input-group input {
    width: 100%;
    padding: 12px 15px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 8px;
    color: white;
    font-family: inherit;
}

.input-group input::placeholder {
    color: rgba(255, 255, 255, 0.6);
}

.auth-links {
    text-align: center;
    margin-top: 20px;
}

.auth-links a {
    color: #FF0000;
    text-decoration: none;
}

/* Boutons */
.btn-primary {
    background: #FF0000;
    color: white;
    border: none;
    padding: 12px 30px;
    border-radius: 25px;
    font-weight: 600;
    cursor: pointer;
    width: 100%;
    font-family: inherit;
    font-size: 16px;
}

.btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.3);
    padding: 12px 30px;
    border-radius: 25px;
    font-weight: 600;
    cursor: pointer;
    width: 100%;
    margin-bottom: 10px;
    font-family: inherit;
    font-size: 16px;
}

/* Profil */
.profile-info {
    background: rgba(255, 255, 255, 0.1);
    padding: 20px;
    border-radius: 15px;
    margin-bottom: 20px;
    text-align: center;
}

.profile-avatar {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    margin: 0 auto 15px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
}

/* Chat */
.conversations-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.conversation-card {
    display: flex;
    align-items: center;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    padding: 15px;
    cursor: pointer;
}

.conversation-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    margin-right: 15px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.conversation-info {
    flex: 1;
}

.conversation-name {
    font-weight: 600;
    margin-bottom: 5px;
}

.last-message {
    font-size: 14px;
    opacity: 0.8;
}

/* Badges de notification */
.notification-badge {
    background: #FF0000;
    color: white;
    border-radius: 50%;
    width: 20px;
    height: 20px;
    font-size: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: -5px;
    right: -5px;
}

/* Animations */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

.fade-in {
    animation: fadeIn 0.3s ease-out;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}

.pulse {
    animation: pulse 0.3s ease-in-out;
}

/* Responsive */
@media (max-width: 360px) {
    .app-container {
        max-width: 360px;
    }
    
    .game-card {
        padding: 10px;
        height: 100px;
    }
    
    .game-logo {
        width: 70px;
        height: 70px;
    }
}
